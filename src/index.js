import React from "react";
import ReactDOM from "react-dom";

import { Table1 } from "./Base";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <Table1 />
  </React.StrictMode>,
  rootElement
);
